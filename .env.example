# Postgres connection (Neon, Supabase, or Azure Database for PostgreSQL)
DATABASE_URL="postgresql://user:password@host:5432/sabiathlon?sslmode=require"

# Auth.js — generate with `npx auth secret`
AUTH_SECRET=""

# Base URL of this app (used for auth callbacks)
NEXTAUTH_URL="http://localhost:3000"
